// ============================================================
// AUTO-GENERATED PLACEHOLDER — do not hand-edit application types here.
//
// Once Shubham's FastAPI surface (P3-001…P3-011) is live, regenerate with:
//
//   NEXT_PUBLIC_API_URL=http://localhost:8000 npm run gen:api
//
// That overwrites this file with `openapi-typescript` output (a single
// `paths` interface keyed by URL, plus a `components.schemas` map).
//
// Until then, this stub keeps the importing code compiling so we can
// scaffold the data layer without a live backend.
// ============================================================

export interface paths {
  // Filled in by openapi-typescript when /openapi.json is reachable.
  [path: string]: unknown;
}

export interface components {
  schemas: Record<string, unknown>;
}

export type operations = Record<string, unknown>;
